# First Slice Data

This folder stages the first playable NPC loop before Unity assets are authored.

P0 target: `npc_lena` -> `mission_lena_001` -> obituary -> yellow page lookup -> phone call -> one dialogue tree with DeepRedemption, DelayRewrite, and CallFailed reachable.
